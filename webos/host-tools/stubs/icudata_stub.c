/* Stub ICU data — just enough symbols to satisfy libicuuc's dynamic link.
 * LLIntSettingsExtractor never actually calls ICU Unicode functions. */
int icudt67_dat = 0;
void u_init_67(void **status) {}
void udata_open_67(void) {}
